<UserControl x:Class="batteryQI.Views.UserControls.ManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">

    <Viewbox Stretch="Uniform">
        <Grid Width="1280" Height="720">
            <Grid Margin="10 50">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <!-- Monthly Inspection Allocation -->
                <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="0 0 0 10">
                    <TextBlock Text="월 검사 할당량" VerticalAlignment="Center" Margin="0 0 5 0" FontSize="25" FontWeight="SemiBold"/>
                    <TextBox Text="{Binding NewWorkAmount, UpdateSourceTrigger=PropertyChanged}"
                     x:Name="InspectionLimitBox" Width="150" VerticalAlignment="Center" Margin="20 0 5 0" Height="auto" FontSize="20"/>
                    <TextBlock Text="건" VerticalAlignment="Center" Margin="0 0 10 0" FontSize="25" FontWeight="SemiBold"/>
                    <Button Content="저장"
                    Width="70" Height="40"
                    VerticalAlignment="Center"
                    Style="{StaticResource commonButton}" Margin="5 0 0 0" FontSize="20"        
                    Command="{Binding SaveButton_ClickCommand}" />
                </StackPanel>

                <!-- Manufacturer List -->
                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="0 0 0 5">
                        <TextBlock Text="제조사 리스트" FontWeight="Bold" VerticalAlignment="Center" FontSize="25"/>
                        <TextBox Text="{Binding NewManufacName, UpdateSourceTrigger=PropertyChanged}"
                         Width="150" Height="auto" VerticalAlignment="Center" Margin="32 0 5 0" FontSize="20"/>
                        <Button Content="추가" FontSize="20" Width="70" Height="40" VerticalAlignment="Center" Margin="42 0 0 0"
                                Style="{StaticResource commonButton}" Command="{Binding ManufactInsertCommand}" />
                        <Button Content="비활성화/재활성화" FontSize="20" Width="200" Height="40" VerticalAlignment="Center" Margin="21 0 0 0"
                                Style="{StaticResource commonButton}" Command="{Binding ManufacDeActiveReActiveCommand}" 
                                CommandParameter="{Binding ElementName=ManufactureDict, Path=SelectedItem}"/>
                    </StackPanel>

                    <ListBox Name="ManufactureDict" ItemsSource="{Binding ManufacList, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid>
                                    <Grid.Style>
                                        <Style TargetType="Grid">
                                            <Style.Triggers>
                                                <!--Active가 true일 때만 제조사 Grid 표시-->
                                                <DataTrigger Binding="{Binding Active}" Value="False">
                                                    <Setter Property="Opacity" Value="0.5" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Grid.Style>

                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="*" />
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="제조사명: " Margin="0 8 5 0" FontSize="22"/>
                                    <TextBlock Grid.Column="1" Text="{Binding Name}" Margin="0 8 5 0" FontSize="22" FontWeight="Bold"/>
                                    <TextBlock Grid.Column="2" Text="- Id: " Margin="0 8 5 0" FontSize="22"/>
                                    <TextBlock Grid.Column="3" Text="{Binding Id}" Margin="0 8 5 0" FontSize="22" FontWeight="SemiBold" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </Grid>
            </Grid>
        </Grid>
    </Viewbox>
</UserControl>