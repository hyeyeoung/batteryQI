<UserControl x:Class="batteryQI.Views.UserControls.ManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">

    <Viewbox Stretch="Uniform">
        <Grid Width="1280" Height="720">

            <Grid Margin="10 50">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

        <!-- Monthly Inspection Allocation -->
        <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="-55,0,55,0">
            <TextBlock Text="월 검사 할당량: " FontWeight="Bold" VerticalAlignment="Center" Margin="0 0 5 0" />
            <TextBox Text="{Binding NewWorkAmount, UpdateSourceTrigger=LostFocus}"
                     x:Name="InspectionLimitBox" Width="150" VerticalAlignment="Center" Margin="0 0 5 0" TextChanged="InspectionLimitBox_TextChanged" />
            <TextBlock Text="건" VerticalAlignment="Center" Margin="0 0 10 0" />
            <Button Content="저장"
                    Width="60" Height="30"
                    VerticalAlignment="Center"
                    Style="{StaticResource commonButton}" Margin="5 0 0 0" FontSize="20"        
                    Command="{Binding SaveButton_ClickCommand}" />
                </StackPanel>

                <!-- Manufacturer List -->
                <Grid Grid.Row="1">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="0 0 0 5">
                        <TextBlock Text="제조사 리스트" FontWeight="Bold" VerticalAlignment="Center" FontSize="25"/>
                        <TextBox Text="{Binding ManufacName, UpdateSourceTrigger=PropertyChanged}"
                         Width="150" Height="auto" VerticalAlignment="Center" Margin="32 0 5 0" FontSize="20"/>
                        <Button Content="추가" FontSize="20" Width="70" Height="40" VerticalAlignment="Center" Margin="42 0 0 0"
                                Style="{StaticResource commonButton}"
                        Command="{Binding ManufactInsertCommand}" />
                    </StackPanel>

            <!--<StackPanel Orientation="Horizontal" VerticalAlignment="Top" Margin="0 0 0 5">
                <TextBlock Text="제조사 리스트:" FontWeight="Bold" VerticalAlignment="Center" Margin="0 0 5 0" />
                <TextBox Text="{Binding ManufacName, UpdateSourceTrigger=PropertyChanged}"
                         Width="150" VerticalAlignment="Center" Margin="0 0 5 0" />
                <Button Content="+" Width="30" Height="30" VerticalAlignment="Center"
                        Command="{Binding ManufactInsertCommand}" />
            </StackPanel>-->

            <ListBox ItemsSource="{Binding ManufacDict, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="{Binding Key, StringFormat='제조사명: {0}'}" Margin="0 8 5 0" FontWeight="Bold"/>
                            <TextBlock Grid.Column="1" Text="{Binding Value, StringFormat='- ID: {0}'}" Margin="0 8 5 0" FontWeight="SemiBold" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Grid>
    </Grid>
</UserControl>
